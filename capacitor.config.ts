import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.angularTp.app',
  appName: 'angular-tp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
